## 验收标准（EARS 语法）

- WHEN 用户提交正确的注册信息 THEN 系统创建账号、发送验证邮件、返回JWT token。 - WHEN 邮箱已存在 THEN 返回 409 Conflict + "邮箱已被注册"。

## 非功能约束

每个模块可以列出与功能相关的非功能需求：

可靠性：操作失败后的回滚机制

安全性：数据权限、访问控制

可用性：界面友好度、易操作性

## 附加说明

业务规则：如“同一用户不能重复创建同名合集”

边界情况：如“如果网络异常，提示重试”